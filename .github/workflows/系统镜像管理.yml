# 系统镜像管理 - 删除 Release 并触发构建

name: 系统镜像管理

# 手动触发
on:
  workflow_dispatch:
    inputs:
      action:
        description: '操作类型'
        required: true
        default: 'build'
        type: choice
        options:
          - build
          - delete
      delete_all:
        description: '删除所有版本的 Release (仅当 action 为 delete 时生效)'
        required: false
        default: false
        type: boolean
      system_type:
        description: '系统类型'
        required: false
        default: 'debian-desktop'
        type: choice
        options:
          - debian-desktop
          - debian-server
          - ubuntu-desktop
          - ubuntu-server
      kernel_version:
        description: '内核版本号 (仅当 action 为 build 时生效)'
        required: false
        default: '6.18'
      desktop_environment:
        description: '桌面环境 (仅当 action 为 build 时生效)'
        required: false
        default: 'phosh-full'
        type: choice
        options:
          - ''
          - phosh-core
          - phosh-full
          - phosh-phone

# 工作流配置
jobs:
  manage-releases:
    # 在 Ubuntu 24.04 ARM 上运行
    runs-on: ubuntu-24.04-arm
    
    # 设置权限
    permissions:
      contents: write
    
    steps:
      # 检出代码
      - name: 检出代码
        uses: actions/checkout@v4
      
      # 删除或构建
      - name: 删除或构建
        run: |
          if [ "${{ inputs.action }}" = "delete" ]; then
            if [ "${{ inputs.delete_all }}" = "true" ]; then
              echo "删除所有系统镜像 Release..."
              # 删除所有系统镜像 Release（不包括内核）
              gh release list --limit 100 | grep -E "(debian|ubuntu)-(desktop|server)" | awk '{print $2}' | xargs -I {} gh release delete --yes
            else
              echo "删除 ${{ inputs.system_type }} Release..."
              # 删除指定系统类型的 Release
              gh release list --limit 100 | grep "${{ inputs.system_type }}" | awk '{print $2}' | xargs -I {} gh release delete --yes
            fi
          elif [ "${{ inputs.action }}" = "build" ]; then
            echo "触发 ${{ inputs.system_type }} 构建..."
            # 触发构建工作流
            gh workflow run "构建系统镜像" -f system_type="${{ inputs.system_type }}" -f kernel_version="${{ inputs.kernel_version }}" -f desktop_environment="${{ inputs.desktop_environment }}"
          else
            echo "未知操作: ${{ inputs.action }}"
            exit 1
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
